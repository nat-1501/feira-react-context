import { BrowserRouter, Switch, Route } from 'react-router-dom';

function Router() {
    return (
        <BrowserRouter>
            <Switch>
                <Router></Router>
            </Switch>
        </BrowserRouter>
    )
}

export default Router;